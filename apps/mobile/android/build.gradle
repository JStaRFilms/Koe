// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

def androidBuildRoot = new File(System.getProperty("java.io.tmpdir"), "koe-android-build")

subprojects { subproject ->
  subproject.buildDir = new File(androidBuildRoot, subproject.name)

  subproject.plugins.withId("com.android.application") {
    subproject.android.externalNativeBuild.cmake.buildStagingDirectory = new File(androidBuildRoot, "${subproject.name}-cxx")
  }

  subproject.plugins.withId("com.android.library") {
    subproject.android.externalNativeBuild.cmake.buildStagingDirectory = new File(androidBuildRoot, "${subproject.name}-cxx")
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
